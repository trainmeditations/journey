language: go

go: 
  - 1.8.3

notifications:
  email: false

gobuild_args: -x -v --ldflags '-extldflags "-static"'

env:
  global:
    - GIMME_ARCH=amd64
  matrix:
    - GIMME_OS=linux
    - GIMME_OS=windows
